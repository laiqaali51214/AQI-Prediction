# Streamlit Secrets Configuration
# Copy this file to .streamlit/secrets.toml and fill in your values
# For Streamlit Cloud: Add these secrets in the dashboard under "Secrets"

# FastAPI Backend URL
FASTAPI_URL = "https://your-api.railway.app"

# MongoDB Connection String (if needed directly in dashboard)
# Note: Usually only needed if dashboard connects to MongoDB directly
# If dashboard only uses API, this is optional
MONGODB_URI = "mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority"
