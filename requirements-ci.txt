# CI/CD Requirements - Minimal dependencies for GitHub Actions
# CPU-only versions to save disk space

# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
python-dotenv>=1.0.0
pyyaml>=6.0

# API clients
requests>=2.31.0

# Database
pymongo>=4.6.0
motor>=3.3.0

# Machine Learning (CPU-only versions)
scikit-learn>=1.3.0
# XGBoost - explicitly install CPU version
xgboost>=2.0.0
# LightGBM - CPU version
lightgbm>=4.0.0

# Web Application (not needed for pipelines, but keep for API)
fastapi>=0.104.0
uvicorn>=0.24.0

# Utilities
# python-dateutil>=2.8.0  # Not needed - pandas installs it automatically
# pytz>=2023.3  # Not needed - pandas installs it automatically
